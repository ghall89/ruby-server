version: "3"

tasks:
  dev:
    desc: "Start backend and frontend"
    deps: [backend, frontend]

  backend:
    dir: backend
    cmd: bundle exec rackup

  frontend:
    dir: frontend
    cmd: pnpm dev
